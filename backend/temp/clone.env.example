# Copy to backend/temp/clone.env (gitignored). Used by scripts/clone_dev_db_docker.sh
#
# Remote dev warehouse (read-only user). URL-encode special chars in password.
# SOURCE_DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require

SOURCE_DATABASE_URL=

# Local Docker target (defaults match docker-compose.yml)
LOCAL_DATABASE_URL=postgresql://localdev:localdev@localhost:5433/investor_db_local
