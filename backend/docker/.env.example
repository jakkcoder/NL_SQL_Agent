# Copy to backend/docker/.env (optional overrides for docker compose)
LOCAL_PG_PORT=5433
LOCAL_PG_USER=localdev
LOCAL_PG_PASSWORD=localdev
LOCAL_PG_DATABASE=investor_db_local

# pgAdmin (http://127.0.0.1:5050)
PGADMIN_PORT=5050
PGADMIN_EMAIL=local@nl-sql-agent.dev
PGADMIN_PASSWORD=localdev
